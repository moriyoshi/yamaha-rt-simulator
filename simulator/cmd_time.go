package simulator

import "context"

var CmdTime = &CommandSpec{
	[]Token{TTime}, 1,
	func(_ context.Context, sess *SimulatorSession, tis []TokenInstance) error {
		if !sess.Enabled {
			return AdministratorUseOnly
		}
		return nil
	},
}

